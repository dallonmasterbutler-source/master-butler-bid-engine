# Cloud-only dependencies (local code stays 100% standard library):
#  psycopg — lets the Render service talk to Postgres
#  pillow  — image resize in the cloud (the Mac uses built-in sips)
psycopg[binary]>=3.1
pillow>=10
